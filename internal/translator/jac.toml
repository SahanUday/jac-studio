[project]
name = "jac-studio-translator"
version = "0.1.0"
description = "TS->Jac porting tool for jac-studio: manifest, eligibility guard, extraction, verification"
entry-point = "main.jac"
kind = "cli"
jac-version = "==0.36.1"

[dependencies]
tomli-w = "*"

[dev-dependencies]
watchdog = ">=3.0.0"

[test]
directory = "tests"
