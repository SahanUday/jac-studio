# All six modules below are ported and passing, but as of 2026-08-25 none of them are wired into
# the running app: the editor engine now embeds the real `monaco-editor` npm package instead of
# this native port (docs/architecture.md's Editor Core section, docs/roadmap.md's Phase 1 update,
# tracker entry 2026-08-25-editor-core-decision-reversed-to-monaco). `jac_path`/`test_jac_path`
# below are stale -- the files were `git mv`'d to `internal/native-editor-archive/src/editor/model/`
# and no longer exist at the paths recorded here. Left as `status = "landed"` rather than rewritten
# to a new status, since they *did* land and pass verification -- the manifest's job is recording
# what was true when a module was ported, not tracking the live app's current composition. Kept
# for the archive's own possible future revival; do not treat these as evidence the editor core is
# translator-built today.

[[modules]]
id = "prefix-sum-computer"
upstream_path = "src/vs/editor/common/model/prefixSumComputer.ts"
upstream_commit = "00cc2df8db2d5c4aee55d4ad14f0835da2e4729a"
jac_path = "../../src/editor/model/prefix_sum_computer.jac"
test_upstream_path = "src/vs/editor/test/common/viewModel/prefixSumComputer.test.ts"
test_jac_path = "../../src/editor/model/prefix_sum_computer.test.jac"
status = "landed"
risk_tier = "low"
verification = "tests"
phase = 0
notes = "Both PrefixSumComputer and ConstantTimePrefixSumComputer ported; 48/48 ported tests pass. First real translation -- see docs/phases/phase-0-foundations.md and the challenge tracker for findings hit along the way."

[[modules]]
id = "interval-tree"
upstream_path = "src/vs/editor/common/model/intervalTree.ts"
upstream_commit = "00cc2df8db2d5c4aee55d4ad14f0835da2e4729a"
jac_path = "../../src/editor/model/interval_tree.jac"
test_upstream_path = "src/vs/editor/test/common/model/intervalTree.test.ts"
test_jac_path = "../../src/editor/model/interval_tree.test.jac"
status = "landed"
risk_tier = "low"
verification = "tests"
phase = 0
notes = "Full red-black tree with lazy delta offsets + maxEnd augmentation ported faithfully; 25/25 ported test blocks pass (18 generated differential-testing regressions against a naive Oracle, a forced-delta-overflow case, 5 Cormen textbook search cases, and the 208-case nodeAcceptEdit table). options/range (ModelDecorationOptions/Range, both unported) are untyped placeholders; set_options() itself deliberately not ported -- see the module docstring."

[[modules]]
id = "piece-tree-base"
upstream_path = "src/vs/editor/common/model/pieceTreeTextBuffer/pieceTreeBase.ts"
upstream_commit = "a4ba39c710a50b04de75cbd55aef093e28266115"
jac_path = "../../src/editor/model/piece_tree_base.jac"
test_upstream_path = "src/vs/editor/test/common/model/pieceTreeTextBuffer/pieceTreeTextBuffer.test.ts"
test_jac_path = "../../src/editor/model/piece_tree_base.test.jac"
status = "landed"
risk_tier = "foundational"
verification = "tests"
phase = 1
notes = "rbTreeBase.ts merged into this file -- two Jac files importing each other's types still pass jac check but silently lose native compilation (2026-08-23-circular-import-degrades-native-lowering). find_matches_in_node/find_matches_line_by_line wired in, backed by text_model_search.jac's Searcher/create_find_match/is_valid_match (textModelSearch.ts subset). 81 tests passing, including all 4 real chunk-based-search cases from pieceTreeTextBuffer.test.ts. One of those (#45892, searching an empty document) proved node_at2's declared non-nullable return type was optimistic -- a genuinely reachable branch, not dead code; node_at2 now returns NodePosition | None for real. Dependencies landed alongside: position.jac, range.jac, word_character_classifier.jac, char_code.jac, strings.jac, model_types.jac."

[[modules]]
id = "piece-tree-text-buffer"
upstream_path = "src/vs/editor/common/model/pieceTreeTextBuffer/pieceTreeTextBuffer.ts"
upstream_commit = "f9b86d8e77d440f1dcc79b17641b96fe98be832e"
jac_path = "../../src/editor/model/piece_tree_text_buffer.jac"
test_upstream_path = "src/vs/editor/test/common/model/pieceTreeTextBuffer/pieceTreeTextBuffer.test.ts"
test_jac_path = "../../src/editor/model/piece_tree_text_buffer.test.jac"
status = "landed"
risk_tier = "low"
verification = "tests"
phase = 1
notes = "The ITextBuffer wrapper: apply_edits (position-mapping edit operations), BOM/RTL/unusual-line-terminator bookkeeping, EOL-preference-aware reads. Event/disposable machinery, undo/redo bookkeeping (textModel.ts's job), and the >=1000-op OOM-workaround batching are deliberate documented scope cuts -- see the module docstring. 99 tests passing."

[[modules]]
id = "piece-tree-text-buffer-builder"
upstream_path = "src/vs/editor/common/model/pieceTreeTextBuffer/pieceTreeTextBufferBuilder.ts"
upstream_commit = "6b924c51528e663dda5091a1493229a361676aca"
jac_path = "../../src/editor/model/piece_tree_text_buffer_builder.jac"
test_upstream_path = "src/vs/editor/test/common/model/pieceTreeTextBuffer/pieceTreeTextBuffer.test.ts"
test_jac_path = "../../src/editor/model/piece_tree_text_buffer_builder.test.jac"
status = "landed"
risk_tier = "low"
verification = "tests"
phase = 1
notes = "Chunk accumulation, EOL auto-detection (majority-vote over cr/lf/crlf, via piece_tree_base.jac's create_line_starts_with_counts), BOM stripping, RTL/unusual-line-terminator flagging, surrogate/CR chunk-boundary handling. Holds the full ported pieceTreeTextBuffer.test.ts suite (197 tests) minus the 3 search-dependent cases, which are ported against PieceTreeBase directly in piece_tree_base.jac instead."

[[modules]]
id = "text-model-search"
upstream_path = "src/vs/editor/common/model/textModelSearch.ts"
upstream_commit = "6b924c51528e663dda5091a1493229a361676aca"
jac_path = "../../src/editor/model/text_model_search.jac"
test_upstream_path = ""
test_jac_path = "../../src/editor/model/text_model_search.test.jac"
status = "landed"
risk_tier = "low"
verification = "tests"
phase = 1
notes = "Scoped subset only: Searcher, create_find_match, is_valid_match/left_is_word_bounday/right_is_word_bounday -- exactly what piece_tree_base.jac's find_matches_in_node/find_matches_line_by_line need. TextModelSearch/SearchParams/LineFeedCounter (model-wide orchestration, above the piece-tree layer) not ported -- belongs with a future TextModel port. No dedicated upstream test file for this subset; direct tests written against real usage shapes (10 tests)."
